<Window x:Class="Netswitch.UI.Windows.DeviceListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:conv="clr-namespace:Netswitch.UI.Converters"
        Title="Connected Devices"
        Height="600"
        Width="900"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource NeutralBackgroundBrush}"
        Foreground="{DynamicResource NeutralForegroundBrush}">
    <Window.Resources>
        <conv:DeviceStatusToBrushConverter x:Key="DeviceStatusBrush" />
        <conv:DeviceStatusTextConverter x:Key="DeviceStatusText" />
        <conv:DateTimeOffsetToLocalConverter x:Key="DateTimeOffsetToLocalConverter" />
    </Window.Resources>
    
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="💻" FontSize="32" VerticalAlignment="Center" Margin="0,0,12,0" />
            <StackPanel>
                <TextBlock Text="All Connected Devices" FontSize="24" FontWeight="Bold" />
                <TextBlock Text="{Binding DeviceCountText}" 
                          FontSize="14" 
                          Foreground="{DynamicResource MutedForegroundBrush}" 
                          Margin="0,4,0,0" />
            </StackPanel>
        </StackPanel>

        <!-- Filters/Stats -->
        <Border Grid.Row="1" 
                Background="#11475569" 
                CornerRadius="8" 
                Padding="16,12" 
                Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Online" FontSize="11" Foreground="{DynamicResource MutedForegroundBrush}" />
                    <TextBlock Text="{Binding OnlineCount}" FontSize="20" FontWeight="Bold" Foreground="#10b981" />
                </StackPanel>
                
                <StackPanel Grid.Column="1">
                    <TextBlock Text="Offline" FontSize="11" Foreground="{DynamicResource MutedForegroundBrush}" />
                    <TextBlock Text="{Binding OfflineCount}" FontSize="20" FontWeight="Bold" Foreground="#ef4444" />
                </StackPanel>
                
                <StackPanel Grid.Column="2">
                    <TextBlock Text="Total" FontSize="11" Foreground="{DynamicResource MutedForegroundBrush}" />
                    <TextBlock Text="{Binding TotalCount}" FontSize="20" FontWeight="Bold" />
                </StackPanel>
                
                <StackPanel Grid.Column="3">
                    <TextBlock Text="Avg Latency" FontSize="11" Foreground="{DynamicResource MutedForegroundBrush}" />
                    <TextBlock Text="{Binding AverageLatency}" FontSize="20" FontWeight="Bold" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- Device List -->
        <Border Grid.Row="2" 
                Background="#11475569" 
                CornerRadius="12" 
                Padding="0">
            <ListView ItemsSource="{Binding Devices}"
                      Background="Transparent"
                      BorderThickness="0"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                      ScrollViewer.VerticalScrollBarVisibility="Auto">
                <ListView.Resources>
                    <Style TargetType="GridViewColumnHeader">
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="Foreground" Value="{DynamicResource MutedForegroundBrush}" />
                        <Setter Property="FontWeight" Value="SemiBold" />
                        <Setter Property="FontSize" Value="12" />
                        <Setter Property="Padding" Value="12,12,12,16" />
                        <Setter Property="BorderThickness" Value="0,0,0,1" />
                        <Setter Property="BorderBrush" Value="#33475569" />
                    </Style>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Padding" Value="12,14" />
                        <Setter Property="Margin" Value="0" />
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="Foreground" Value="{DynamicResource NeutralForegroundBrush}" />
                        <Setter Property="BorderThickness" Value="0,0,0,1" />
                        <Setter Property="BorderBrush" Value="#11475569" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListViewItem">
                                    <Border Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            Padding="{TemplateBinding Padding}">
                                        <GridViewRowPresenter Content="{TemplateBinding Content}"
                                                            Columns="{TemplateBinding GridView.ColumnCollection}">
                                            <GridViewRowPresenter.Resources>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="{DynamicResource NeutralForegroundBrush}" />
                                                    <Setter Property="FontSize" Value="13" />
                                                </Style>
                                            </GridViewRowPresenter.Resources>
                                        </GridViewRowPresenter>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#22475569" />
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="#334F46E5" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListView.Resources>
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Status" Width="95">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Ellipse Width="10" Height="10" VerticalAlignment="Center"
                                                Fill="{Binding IsOnline, Converter={StaticResource DeviceStatusBrush}}" />
                                        <TextBlock Text="{Binding IsOnline, Converter={StaticResource DeviceStatusText}}" 
                                                  Margin="8,0,0,0" 
                                                  FontWeight="SemiBold" />
                                    </StackPanel>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        
                        <GridViewColumn Header="IP Address" Width="135">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding IpAddress}" 
                                              FontFamily="Consolas"
                                              FontWeight="SemiBold" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        
                        <GridViewColumn Header="Hostname" Width="200">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding HostName}" 
                                              TextTrimming="CharacterEllipsis"
                                              ToolTip="{Binding HostName}"
                                              FontWeight="SemiBold" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        
                        <GridViewColumn Header="MAC Address" Width="140">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding MacAddress}" 
                                              FontFamily="Consolas"
                                              FontSize="12" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        
                        <GridViewColumn Header="Latency" Width="80">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding LatencyMs, StringFormat='{}{0} ms'}" 
                                              TextAlignment="Right"
                                              HorizontalAlignment="Right" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        
                        <GridViewColumn Header="First Seen" Width="130">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock TextAlignment="Right" HorizontalAlignment="Right">
                                        <TextBlock.Text>
                                            <Binding Path="FirstSeen" StringFormat="{}{0:g}">
                                                <Binding.Converter>
                                                    <conv:DateTimeOffsetToLocalConverter />
                                                </Binding.Converter>
                                            </Binding>
                                        </TextBlock.Text>
                                    </TextBlock>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        
                        <GridViewColumn Header="Last Seen" Width="130">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock TextAlignment="Right" HorizontalAlignment="Right">
                                        <TextBlock.Text>
                                            <Binding Path="LastSeen" StringFormat="{}{0:g}">
                                                <Binding.Converter>
                                                    <conv:DateTimeOffsetToLocalConverter />
                                                </Binding.Converter>
                                            </Binding>
                                        </TextBlock.Text>
                                    </TextBlock>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>

        <!-- Footer -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Close" 
                    Click="CloseButton_Click"
                    Padding="24,12"
                    Background="#4F46E5"
                    Foreground="White"
                    BorderThickness="0"
                    FontWeight="SemiBold"
                    FontSize="14"
                    Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="8"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#6366F1" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</Window>
